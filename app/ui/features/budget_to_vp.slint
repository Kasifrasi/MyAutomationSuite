import {
    Button,
    LineEdit,
    GridBox,
    Palette,
    StandardTableView,
    ScrollView,
} from "std-widgets.slint";
import {
    Card,
    StatusBanner,
    SettingCheckBox,
    PasswordField,
} from "../shared/components.slint";
import { VorpruefungState } from "../shared/state.slint";

// Seite: Aus Budgets Prüfvorlagen (Vorpruefung) erzeugen.
export component VorpruefungPage inherits VerticalLayout {
    spacing: 10px;

    // --- 1. FIXED HEADER (Status & Pfade) ---
    VerticalLayout {
        spacing: 10px;

        if VorpruefungState.status-type != "idle": StatusBanner {
            status-type: VorpruefungState.status-type;
            status-message: VorpruefungState.status-message;
            dismiss => {
                VorpruefungState.dismiss-status();
            }
        }

        HorizontalLayout {
            spacing: 16px;
            // Quelle (Budget-Ordner)
            HorizontalLayout {
                horizontal-stretch: 1;
                spacing: 8px;
                LineEdit {
                    text <=> VorpruefungState.src-folder;
                    read-only: true;
                    placeholder-text: "* Budget-Ordner...";
                    horizontal-stretch: 1;
                }

                Button {
                    text: "Quelle...";
                    clicked => {
                        VorpruefungState.select-src();
                    }
                }
            }
            // Ausgabe
            HorizontalLayout {
                horizontal-stretch: 1;
                spacing: 8px;
                padding-right: 12px;
                LineEdit {
                    text <=> VorpruefungState.out-folder;
                    read-only: true;
                    placeholder-text: "* Ausgabeordner...";
                    horizontal-stretch: 1;
                }

                Button {
                    text: "Ausgabe...";
                    clicked => {
                        VorpruefungState.select-out();
                    }
                }
            }
        }
    }

    // --- 2. SCROLLABLE CONTENT (Settings & Tabelle) ---
    ScrollView {
        vertical-stretch: 1;

        VerticalLayout {
            spacing: 12px;
            padding-right: 12px;

            if VorpruefungState.show-settings: Card {
                title: "Erweiterte Einstellungen";
                VerticalLayout {
                    spacing: 12px;

                    // Dateiname / Muster
                    HorizontalLayout {
                        spacing: 16px;
                        alignment: start;
                        VerticalLayout {
                            spacing: 4px;
                            Text {
                                text: "* Dateinamens-Muster";
                                font-size: 13px;
                                font-weight: 600;
                                opacity: 0.8;
                            }

                            LineEdit {
                                text <=> VorpruefungState.name;
                                placeholder-text: "* z.B. Pruefvorlage_{pn}_{la}.xlsx";
                                width: 320px;
                                edited => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        VerticalLayout {
                            spacing: 4px;
                            alignment: center;
                            Text {
                                text: "💡 Platzhalter:\n{pn} = Projektnummer\n{la} = Sprache\n{version} = Version aus Budget\n{i} = Duplikate-Zähler";
                                font-size: 12px;
                                opacity: 0.7;
                                vertical-alignment: center;
                            }
                        }
                    }

                    // Sichtbarkeit & Schutz
                    GridBox {
                        spacing: 10px;
                        Row {
                            SettingCheckBox {
                                text: "Blattschutz";
                                value <=> VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Mappenschutz";
                                value <=> VorpruefungState.protect-workbook;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }
                    }

                    // Passwörter
                    HorizontalLayout {
                        spacing: 8px;
                        PasswordField {
                            label: "Blatt-Passwort";
                            password <=> VorpruefungState.sheet-password;
                            is-enabled: VorpruefungState.protect-sheet;
                            changed => {
                                VorpruefungState.save-settings();
                            }
                        }

                        PasswordField {
                            label: "Mappen-Passwort";
                            password <=> VorpruefungState.workbook-password;
                            is-enabled: VorpruefungState.protect-workbook;
                            changed => {
                                VorpruefungState.save-settings();
                            }
                        }
                    }

                    // Berechtigungen
                    Text {
                        text: "Erlaubte Aktionen bei Blattschutz";
                        font-size: 13px;
                        font-weight: 600;
                        opacity: VorpruefungState.protect-sheet ? 0.6 : 0.3;
                    }

                    GridBox {
                        spacing: 2px;
                        Row {
                            SettingCheckBox {
                                text: "Gesperrte Zellen wählen";
                                value <=> VorpruefungState.sheet-permissions.select-locked-cells;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Spalten löschen";
                                value <=> VorpruefungState.sheet-permissions.delete-columns;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Freie Zellen wählen";
                                value <=> VorpruefungState.sheet-permissions.select-unlocked-cells;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Zeilen löschen";
                                value <=> VorpruefungState.sheet-permissions.delete-rows;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Zellen formatieren";
                                value <=> VorpruefungState.sheet-permissions.format-cells;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Sortieren";
                                value <=> VorpruefungState.sheet-permissions.sort;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Spalten formatieren";
                                value <=> VorpruefungState.sheet-permissions.format-columns;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Autofilter";
                                value <=> VorpruefungState.sheet-permissions.auto-filter;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Zeilen formatieren";
                                value <=> VorpruefungState.sheet-permissions.format-rows;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Pivot-Tabellen";
                                value <=> VorpruefungState.sheet-permissions.pivot-tables;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Spalten einfügen";
                                value <=> VorpruefungState.sheet-permissions.insert-columns;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Objekte bearbeiten";
                                value <=> VorpruefungState.sheet-permissions.edit-objects;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Zeilen einfügen";
                                value <=> VorpruefungState.sheet-permissions.insert-rows;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Szenarien";
                                value <=> VorpruefungState.sheet-permissions.edit-scenarios;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }

                        Row {
                            SettingCheckBox {
                                text: "Links einfügen";
                                value <=> VorpruefungState.sheet-permissions.insert-hyperlinks;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }

                            SettingCheckBox {
                                text: "Inhalte schützen";
                                value <=> VorpruefungState.sheet-permissions.contents;
                                is-enabled: VorpruefungState.protect-sheet;
                                changed => {
                                    VorpruefungState.save-settings();
                                }
                            }
                        }
                    }
                }
            }

            // Ergebnistabelle
            Card {
                title: "Ergebnisse";
                min-height: 250px;
                StandardTableView {
                    columns: VorpruefungState.table-columns;
                    rows: VorpruefungState.table-data;
                }
            }
        }
    }

    // --- 3. FIXED FOOTER (Aktionen) ---
    HorizontalLayout {
        padding-top: 5px;
        spacing: 12px;

        Button {
            text: VorpruefungState.show-settings ? "Einstellungen ▲" : "Einstellungen ▼";
            clicked => {
                VorpruefungState.toggle-settings();
            }
        }

        Button {
            text: "Zurücksetzen";
            clicked => {
                VorpruefungState.do-reset();
            }
        }

        Rectangle {
            horizontal-stretch: 1;
        }

        Button {
            text: VorpruefungState.status-type == "pending" ? "Erstellt..." : "Prüfvorlagen erstellen (F5)";
            primary: true;
            enabled: VorpruefungState.src-folder != "" && VorpruefungState.out-folder != "" && VorpruefungState.name != "" && VorpruefungState.status-type != "pending";
            clicked => {
                VorpruefungState.generate();
            }
            width: 240px;
        }
    }
}
