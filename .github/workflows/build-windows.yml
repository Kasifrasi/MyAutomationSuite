name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build on Windows
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"

      - name: Build Go Sidecars
        run: |
          cd sidecars/Excelize
          go build -o generator.exe ./cmd/report_generator
          cd ../Vorpruefung
          go build -o vorpruefung.exe .

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Setup Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "windows-release"
          cache-on-failure: true

      - name: Build GUI
        run: cargo build --release -p app

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: target/release/app.exe
          if-no-files-found: error

      - name: Rename artifact
        run: cp target/release/app.exe target/release/app-x86_64-pc-windows-msvc.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: target/release/app-x86_64-pc-windows-msvc.exe
